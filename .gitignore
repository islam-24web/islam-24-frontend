# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (secrets) — keep example files tracked
.env*
!.env*.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local backup zips (e.g. frontend-source.zip) — never commit
*.zip

# local one-shot seed/admin scripts — never commit
.seed-*.py
.seed-*.json
